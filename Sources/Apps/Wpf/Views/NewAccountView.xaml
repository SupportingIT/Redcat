<UserControl x:Class="Redcat.Communicator.Views.NewAccountView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:prism="http://prismlibrary.com/"
             prism:ViewModelLocator.AutoWireViewModel="True">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition/>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition />
            <RowDefinition />
            <RowDefinition />
            <RowDefinition />
        </Grid.RowDefinitions>
        <Label>Name</Label>
        <TextBox Grid.Column="1" Text="{Binding AccountName}" />
        <Label Grid.Row="1">Protocol</Label>
        <ComboBox Grid.Row="1" Grid.Column="1" ItemsSource="{Binding Protocols}" SelectedValue="{Binding SelectedProtocol}" />
        <ContentControl Grid.Row="2" Grid.ColumnSpan="2" prism:RegionManager.RegionName="ProtocolSettings" />
        <StackPanel Orientation="Horizontal" Grid.Row="3" Grid.ColumnSpan="2">
            <Button Command="{Binding CreateAccountCommand}">OK</Button>
            <Button>Cancel</Button>
        </StackPanel>
    </Grid>
</UserControl>
