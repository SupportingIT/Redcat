<views:MvxWpfView x:Class="Redcat.App.Wpf.Views.HomeView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:views="clr-namespace:MvvmCross.Wpf.Views;assembly=MvvmCross.Wpf"
             xmlns:ma="http://metro.mahapps.com/winfx/xaml/controls"
                  x:Name="Root">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="auto" />
            <ColumnDefinition />
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>            
            <RowDefinition />
        </Grid.RowDefinitions>        
        <ListView Grid.Row="0" ItemsSource="{Binding ProtocolModules}">
            <ListView.ItemTemplate>
                <ItemContainerTemplate>
                    <ma:Tile Content="{Binding ProtocolName}" Command="{Binding ElementName=Root, Path=DataContext.ShowProtocolModuleCommand}" CommandParameter="{Binding}" />
                </ItemContainerTemplate>
            </ListView.ItemTemplate>
        </ListView>
        <ContentControl Grid.Column="1">
            <Rectangle Stroke="Gray">
                
            </Rectangle>
        </ContentControl>
    </Grid>
</views:MvxWpfView>
