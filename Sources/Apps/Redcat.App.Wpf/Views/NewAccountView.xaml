<views:MvxWpfView x:Class="Redcat.App.Wpf.Views.NewAccountView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:views="clr-namespace:Cirrious.MvvmCross.Wpf.Views;assembly=Cirrious.MvvmCross.Wpf">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition/>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition />
            <RowDefinition />
            <RowDefinition />
            <RowDefinition />
        </Grid.RowDefinitions>
        <TextBlock>Name</TextBlock>
        <TextBox Grid.Column="1" Text="{Binding AccountName}" />
        <TextBlock Grid.Row="1">Protocol</TextBlock>
        <ComboBox Grid.Row="1" Grid.Column="1" ItemsSource="{Binding Protocols}" SelectedItem="{Binding SelectedProtocol}" />
        <ContentControl Grid.Row="2" Grid.ColumnSpan="2" x:Name="ProtocolSettings" />
        <StackPanel Orientation="Horizontal" Grid.Row="3" Grid.ColumnSpan="2">
            <Button Command="{Binding CreateAccountCommand}">OK</Button>
            <Button>Cancel</Button>
        </StackPanel>
    </Grid>    
</views:MvxWpfView>
